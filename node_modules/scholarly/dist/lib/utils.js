"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.splitVolumeIssueString = exports.splitJournalVolumeString = void 0;
var splitJournalVolumeString = function (html) {
    for (var idx = 0; idx < html.length; idx++) {
        var char = html[idx];
        if (!isNaN(parseInt(char))) {
            return idx;
        }
    }
    return html.length;
};
exports.splitJournalVolumeString = splitJournalVolumeString;
var splitVolumeIssueString = function (html) {
    var output = {
        volume: undefined,
        issue: undefined,
    };
    var value = "";
    for (var idx = 0; idx < html.length; idx++) {
        var char = html[idx];
        // format 10 (10)
        if (char !== " " && isNaN(parseInt(char))) {
            if (!output.volume) {
                output.volume = parseInt(value);
            }
            else {
                output.issue = parseInt(value);
                return output;
            }
            value = "";
        }
        else {
            value += char;
        }
    }
    return output;
};
exports.splitVolumeIssueString = splitVolumeIssueString;
